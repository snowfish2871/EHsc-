# EHsc文件哈希校验
使用C++作为语言制作。

离线计算所选文件的 MD5 / CRC32 / SHA1 / SHA256 / SHA384 / SHA512

不依赖第三方库。

哈希部分使用 Windows 自带 CryptoAPI，CRC32 自实现。适合 MSVC 直接编译。

说明：MD5 和 SHA1 已不适合作为安全签名算法，但用于文件完整性校验仍常见。若用于安全校验，优先使用 SHA256 或更高。
